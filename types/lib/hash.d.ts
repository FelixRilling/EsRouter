/**
 * Returns if the page URL has a hash part
 *
 * @private
 * @returns {boolean}
 */
declare const hasHash: () => boolean;
/**
 * Returns hash without init-character
 *
 * @private
 * @returns {string}
 */
declare const getHash: () => string;
export { getHash, hasHash };
